# Compact Input Fields

Pill-style text inputs with label inside the field instead of above it. Saves vertical space in Settings.

```
┌──────────────────────────────┐
│ Host │ 100.x.x.x             │
└──────────────────────────────┘
```

**Files:** New `CompactTextField.swift`, update `SettingsView.swift`
