# iOS File Editing {square.and.pencil}

> Read file → edit in-app → preview diff → confirm write. Basic conflict detection (file changed since read). Could add per-project edit permissions.

**Files:** `FileBrowserView.swift`, `FilePreviewView.swift`
