# Stream Parsing Dedup

Use a single canonical stream path in ClaudeCodeRunner to reduce duplicate output. Improve JSON line parsing error handling, emit structured errors. Attach exit status and stderr summaries to final response.

**Files:** `ClaudeCodeRunner.swift`, `ClaudeCodeRunner+Streaming.swift`
