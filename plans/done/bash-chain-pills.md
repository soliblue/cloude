# Bash Chain Pills

Parse `&&` chains in Bash tool inputs into separate linked pills with connector lines between them. Respect quotes when splitting.

**Files:** `ChatView+MessageBubble.swift`, `BashCommandParser.swift`
